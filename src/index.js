import React from "react";
import ReactDOM from "react-dom/client";
import Harshosale from "./harshosale";

const root = ReactDOM.createRoot(document.getElementById("root"));
root.render(
  <React.StrictMode>
    <Harshosale />
  </React.StrictMode>
);
